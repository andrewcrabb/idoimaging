.row
  .col-sm-12.col-md-6
    %h3 Your Programs
    - if @ratings.count > 0
      You are monitoring #{@ratings.count} #{pluralize(@ratings.count, 'program')}:
      %ul
        = content_tag_for(:li, @ratings) do |rating|
          - if rating.program_id
            - program = rating.program
            = link_to(program.name, program_path(program))
    - else
      You are not monitoring any applications.  Any applications you give a rating to, will appear here.

  .col-sm-12.col-md-6
    %h3 Your Account
    %p You can change your email or password, or cancel your account, below.
    %p= link_to "Edit account", edit_user_registration_path
