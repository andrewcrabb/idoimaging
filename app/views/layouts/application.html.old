<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? "#{yield(:title)} : I Do Imaging" : "I Do Imaging" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "foundation" %>
    <%= stylesheet_link_tag "http://cdn.jsdelivr.net/jquery.slick/1.6.0/slick.css" %>
    <%= stylesheet_link_tag "http://cdn.jsdelivr.net/jquery.slick/1.6.0/slick-theme.css" %>

    <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/2.0.0/jquery.min.js"  %>
    <%= javascript_include_tag "jquery.star-rating-svg.js" %>
    <%= stylesheet_link_tag "star-rating-svg.css" %>
    <%= javascript_include_tag "modernizr.js" %>
    <%= javascript_include_tag "https://use.fontawesome.com/3b2399c77f.js" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>

    <% javascript_include_tag "my_rating" %>
    <%= csrf_meta_tags %>
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <header class="header">
      <%= render "shared/navigation" %>
    </header>
    <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
    <% # content_tag :div, value, class: "flash alert alert-info #{key}" %>
    <% end %>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
    <%= render 'layouts/footer' %>
    <!-- Slick -->
    <script type="text/javascript">
    $(document).ready(function(){
if ($('.slick-slider').length ) {
    $('.slick-slider').slick({
    infinite: true,
    slidesToShow: 2,
    slidesToScroll: 1,
    dots: true,
    arrows: true,
    accessibility: true
    });
}
    });
    </script>

    <script type="text/javascript" src="//cdn.jsdelivr.net/jquery.slick/1.6.0/slick.min.js"></script>
    <script type="text/javascript">
    $('.slick-slider').on('click', function(){
    $(this).focus();
    });   </script>



  </body>
</html>